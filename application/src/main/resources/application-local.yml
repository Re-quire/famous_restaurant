spring:
  datasource:
    url: jdbc:mysql://mysql:3306/yummy_db  # docker-compose에서 mysql 서비스 연결
    username: yummy
    password: yummy1234!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
